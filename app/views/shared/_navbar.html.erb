<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Home</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if @chat_rooms %>
        <%= form_tag("", url: chat_rooms_path, role: 'search', method: :get, class: "navbar-form navbar-left") do %>
          <div class="form-group">
            <%= text_field_tag :search, @search_term, placeholder: "Search", class: "form-control" %>
          </div>
        <% end %>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if cookies[:moon] %>
          <li><a href="/sun">Light mode</a></li>
        <% else %>
          <li><a href="/moon">Dark mode</a></li>
        <% end %>
        <%if !current_user%>
          <li><a href="/users/sign_in">Sign in</a></li>
        <%else%>
          <li><a href="/users/all">Users</a></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=current_user.username%><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="/profile/<%=current_user.id%>">Profile</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="/users/sign_out" data-method="delete">Log out</a></li>
            </ul>
          </li>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>